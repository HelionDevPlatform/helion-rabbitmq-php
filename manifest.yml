---
applications:
- name: helion-rabbitmq-php  
  buildpack: https://github.com/heroku/heroku-buildpack-php.git  
  services:
    rabbitmq:
      type: rabbitmq3  
  stackato: 
    hooks:
      pre-staging:
        - curl -sS https://getcomposer.org/installer | php
        - php composer.phar install
